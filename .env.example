# Optional TabPFN comparison. Leave blank and use: python -m src.train --skip-tabpfn
# Get a key at https://ux.priorlabs.ai (accept license in browser first)
# CPU runs on >1k rows need TABPFN_ALLOW_CPU_LARGE_DATASET=1 (train.py sets this)
TABPFN_TOKEN=
TABPFN_ALLOW_CPU_LARGE_DATASET=1
