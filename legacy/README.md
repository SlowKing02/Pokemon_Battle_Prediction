# Legacy (2018)

Original scripts before the 2026 pipeline refactor. Requires CSVs in repo root and manual `Train_Test_Data/` exports.

- `Pokemon_Battle_Match.py` — monolith: feature ETL, RF, LogReg, Keras, Kaggle export
- `CatBoost.py` — CatBoost on pre-exported CSVs

Use `src/` for all new work.
